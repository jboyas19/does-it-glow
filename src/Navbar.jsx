export default function Navbar() {
  return (
    <header>
      <h1>Does it glow?</h1>
      <button
        id="toggle"
        className="material-symbols-outlined"
        aria-label="Toggle light/dark mode"
      >
        dark_mode
      </button>
    </header>
  );
}
